# PR03: Durable sync and historical data model

Branch: `feat/03-sync-data-model`

Dependency: Complete and merge the preceding implementation PR before marking this ready.

Make the database the reliable read model; dashboard loads must not call Meta.

### Required implementation
- Add/migrate Campaign, AdSet, Ad, Creative, DailyInsight and SyncRun; use minor currency units and suitable raw JSON.
- Extract a reusable transactional sync service for metadata, insights, normalisation, upserts and honest run status.
- Support 90-day default backfill plus recurring today/recent attribution-window refresh.
- Ensure idempotency and safe overlap prevention; route cron and manual refresh through the same service.
- Make dashboard state database-only.
- Implement account-timezone-correct today, yesterday, MTD, 7/previous 7, 14/previous 14, 30/previous 30.
- Preserve last good data and expose freshness/failure.

### Tests/acceptance
Cover duplicate sync, delayed conversion updates, failures, overlap, empty account and date/timezone boundaries. After a successful sync, dashboard must load with Meta unavailable and clearly report staleness.

## Execution rules

Read `AGENTS.md`, `docs/MASTER_PLAN.md`, and `docs/IMPLEMENTATION_STATUS.md`. Inspect existing code, preserve useful pieces, keep scope tight, add tests, run lint/typecheck/tests/build plus relevant browser checks, update the tracker, and record any genuine external blocker. Never commit secrets or convert errors into zeroes.
