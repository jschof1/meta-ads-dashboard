# PR10: Production hardening and documentation

Branch: `feat/10-production-ready`

Dependency: Complete and merge the preceding implementation PR before marking this ready.

Make the finished system boringly reliable.

### Required implementation
- Keep Vercel/Turso unless evidence compels change; safely provision/migrate production and configure supported cron/secrets.
- Verify manual sync, auth, AI and integration failure visibility.
- Add safe diagnostics for Meta, AI, HighLevel, stale DB and migrations.
- Rewrite README/setup for actual UKTL architecture, env, Meta result setup, targets, DB/Vercel/cron, HighLevel, auth/rotation, safe writes, API upgrades and recovery.
- Remove nonexistent/outdated upstream instructions.
- Run all automated gates and browser smoke tests.
- Reconcile key metrics manually against Meta using identical dates/attribution; sample-check HighLevel where enabled.

### Acceptance
Production, migrations, cron/manual sync, auth and integrations are verified; docs match reality; manual reconciliation is recorded; all automated and browser gates pass.

## Execution rules

Read `AGENTS.md`, `docs/MASTER_PLAN.md`, and `docs/IMPLEMENTATION_STATUS.md`. Inspect existing code, preserve useful pieces, keep scope tight, add tests, run lint/typecheck/tests/build plus relevant browser checks, update the tracker, and record any genuine external blocker. Never commit secrets or convert errors into zeroes.
