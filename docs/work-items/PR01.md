# PR01: Secure and reproducible baseline

Branch: `feat/01-secure-baseline`

Dependency: No preceding implementation PR.

Make deployment safe before connecting real data.

### Required implementation
- Commit a documented `.env.example`, allow it through `.gitignore`, and validate environment configuration server-side.
- Replace localStorage authentication with signed, expiring, secure HttpOnly cookie sessions using `DASHBOARD_PASSWORD` and `AUTH_SECRET`.
- Protect dashboard pages plus dashboard, AI, plan, refresh and future action APIs. Keep cron separately protected by `CRON_SECRET`.
- Add logout and proportionate login rate limiting.
- Add `typecheck`, unit tests, and CI for install/lint/typecheck/test/build; resolve the Prisma package-version mismatch deliberately.
- Add a protected health surface exposing only safe configuration/database/sync statuses.

### Acceptance
- Unauthenticated data APIs return 401; the old localStorage key grants nothing.
- Login/logout and cron authentication are server verified.
- No secret reaches HTML, browser JS, or logs.
- Clean install, lint, typecheck, unit tests, build and CI pass.

## Execution rules

Read `AGENTS.md`, `docs/MASTER_PLAN.md`, and `docs/IMPLEMENTATION_STATUS.md`. Inspect existing code, preserve useful pieces, keep scope tight, add tests, run lint/typecheck/tests/build plus relevant browser checks, update the tracker, and record any genuine external blocker. Never commit secrets or convert errors into zeroes.
