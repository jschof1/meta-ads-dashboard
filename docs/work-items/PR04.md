# PR04: Convert product to UK Trade Leads

Branch: `feat/04-uktl-domain-model`

Dependency: Complete and merge the preceding implementation PR before marking this ready.

Remove upstream business assumptions without redesigning the core layout.

### Required implementation
- Add typed UKTL configuration for name, locale, funnel terms, optional targets/budgets, evidence and frequency rules; no admin panel.
- Use Meta account currency with `Intl.NumberFormat`, `en-GB`, and account timezone.
- Replace registration/CPR with lead/CPL; remove webinar, enrollment, Business Owners US, Framework Leads, $4,500 and copied USD assumptions.
- Replace campaign context/template with a secret-free UKTL brief explaining the UK trades conversion system and that lead quality beats raw volume.
- Treat targets as unknown when absent and compare historically.
- Remove/disable the misleading linear budget simulator.

### Acceptance
No unsupported upstream terms/economics remain user-facing. Currency/dates are correct, unknown targets are explicit, and all quality gates pass.

## Execution rules

Read `AGENTS.md`, `docs/MASTER_PLAN.md`, and `docs/IMPLEMENTATION_STATUS.md`. Inspect existing code, preserve useful pieces, keep scope tight, add tests, run lint/typecheck/tests/build plus relevant browser checks, update the tracker, and record any genuine external blocker. Never commit secrets or convert errors into zeroes.
